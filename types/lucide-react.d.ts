// Intentionally empty. lucide-react ships its own types via
// `typings: "dist/lucide-react.d.ts"` in its package.json, so no ambient
// override is needed. (We previously had a fallback declaration here that
// only exposed a handful of icons — it ended up masking the real typings.)
export {};
