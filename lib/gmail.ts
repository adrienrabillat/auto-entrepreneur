/**
 * @deprecated Gmail send decommissioned mai 2026 — tout passe par Resend.
 * Garde ce fichier vide pour ne pas casser les anciens imports si une
 * branche oubliée s'y réfère ; à supprimer (`git rm`) une fois sûr que
 * plus rien ne l'importe.
 */
export {};
